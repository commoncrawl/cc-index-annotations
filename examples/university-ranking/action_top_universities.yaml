sql: "SELECT {columns} FROM joined WHERE {where} ORDER BY cwur_world_rank ASC"
columns: "surt_host_name, crawl, university_name, country, cwur_world_rank, cwur_score"
where: "in_cwur = true AND cwur_world_rank IS NOT NULL AND crawl = 'CC-MAIN-2021-49'"
limits:
  count: 25
