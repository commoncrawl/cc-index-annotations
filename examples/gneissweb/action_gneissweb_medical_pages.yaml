sql: "
SELECT
  {columns}
FROM joined
WHERE {where}
"

columns: "url_surtkey, crawl, fetch_time, warc_filename, crawl, gneissweb_technology, gneissweb_science, gneissweb_education, gneissweb_medical"

where: "gneissweb_medical > 0.5"

